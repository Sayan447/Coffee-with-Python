# immutable means that we can't change it
# mutable means we can change it

# username = "hitesh"
# print(username) #hitesh

# username = "chaiaurcode"
# print(username) #chaiaurcode


x = 10
y = x
print("x", x)
print("y" , y)

x = 15
print("y" , y)

# everything is obj in python which is store in the memory

# immutable means we can't change it in the memory, we can also point some other references in the same variable but in the memory it's treated like this.